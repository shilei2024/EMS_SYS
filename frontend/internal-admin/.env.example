# 前端环境变量配置

# API 基础 URL
# 开发环境：使用 /api 通过 Vite 代理到本地网关 (localhost:80)
# 生产环境：设置为完整的网关地址，例如：http://your-domain.com/api 或 https://api.your-domain.com
VITE_API_BASE_URL=/api

# 是否启用 Mock 数据（可选）
VITE_USE_MOCK=false

# 应用标题
VITE_APP_TITLE=电子元器件代理商增值系统

# 应用版本
VITE_APP_VERSION=1.0.0
